{
  "checks": {
    "check-consul-dns-<%= svc.name %>": {
      "aggregate": true,
      "command": "check-dns.rb --domain <%= svc.name %>.service.consul",
      "interval": 60,
      "subscribers": ["roundrobin:sensu-checker"],
      "refresh": 86400,
      "handlers": <% if svc.name.include?('production') %> ["production"] <% else %>["staging"]<% end %>,
      "source": "<%= svc.name %>.service"
    }
  }
}
