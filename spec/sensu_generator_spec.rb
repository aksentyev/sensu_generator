require 'spec_helper'

describe SensuGenerator do
  it 'has a version number' do
    expect(SensuGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
